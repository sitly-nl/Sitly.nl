export enum PaymentType {
    initial = 'initial',
    recurring = 'recurring',
    chargeback = 'chargeback',
    refund = 'refund',
    validation = 'validation',
    setup = 'setup',
    setupProcessing = 'setup_processing',
}
